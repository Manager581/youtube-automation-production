# WVT2FCjhDDY — "Claude Code (Free Plan) + YouTube = $77,000/Month" (zapiwala ai, 12:27, 673K views)

## What the video is
A beginner tutorial for making "stickman doodle" still-image slideshow videos with free tools, wrapped in a bait title. Despite the title, **Claude Code never appears** — the workflow uses the Claude.ai web app free plan with a "master prompt" (pinned comment). The **$77,000/month figure is never mentioned, shown, or supported anywhere in the video**. The real product is the creator's own Chrome extension (Zapi Flow) and zapiwala.ai redirect links on every tool.

## What's actually shown (evidence)
- An unnamed stickman channel: 14 videos, 137K subs, 14.5M views in 2 months, one 7.5M-view video (screenshots only; channel not identified, so unverifiable).
- Workflow demo: master prompt → Claude generates 5 topic ideas → full 5–15 min script → ElevenLabs free (10K credits/mo, voice "Raunak") → FoziScribe.ai transcribes VO and outputs **pause timestamps** → Claude turns the timestamped script into per-line text-to-image prompts → Google Flow (flow.google), image mode, 16:9, 1 output/prompt, **Nano Banana 2** model → Zapi Flow extension bulk-runs 103 prompts (one line break between prompts; 102/103 succeeded) → manual editor sync using timestamps embedded in filenames → Claude generates title/description/tags + 5 thumbnail prompts.
- No revenue, RPM, or analytics evidence of any kind.

## Credibility: LOW
Title is double-bait ("Claude Code" keyword-riding + fabricated revenue number). All description links are zapiwala.ai tracked redirects; the funnel is the creator's extension + "money printing workflow" end-card video. Advice to "create another free account" when FoziScribe credits run out is ToS abuse — tells you how this creator treats platform rules. The one honest, mechanical idea (VO-first, cut scenes on natural voice pauses) is real but presented as a secret; it's standard practice.

## Tactics extracted
1. **Voiceover-first, scene-second; scene boundaries = VO pauses** (detailed with frame-level timestamps ~3:35). CONFIRM — this is exactly the operation's WhisperX word-level alignment + `realign_paper_edit.py` path, done better (local, free, word-level). FoziScribe adds nothing.
2. **Google Flow + Nano Banana 2 as a $0 bulk still-image source** (~6:40). NEW-ish tool datapoint. Memory already flags Flow free tier (50 cr/day, watermark on Veo); image mode w/ Nano Banana 2 may be a usable free still source. Worth one cheap test only.
3. **Third-party Chrome extension for bulk prompt→image→autodownload** (~7:23). IGNORE the extension itself (untrusted, automates your Google account); the operation already owns equivalent browser-automation recipes (Grok clipboard-upload, TikTok automation).
4. **Timestamp-in-filename edit sync** (~9:49). Irrelevant — FFmpeg paper-edit pipeline is fully programmatic.
5. **Claude-generated metadata + 5 thumbnail prompts** (~10:51). CONFIRM/weaker than the 26-tactic titles_thumbnails playbook and thumbnail recipe.
6. **Meta-observation**: the video's own packaging (tool name + "free plan" + big revenue number) pulled 673K views — a niche-specific title formula, not transferable to creature/doc channels.

## Policy notes
Zero policy discussion — a red flag in itself. The taught format (mass-produced still-image slideshow + stock AI voice + templated prompts) is the archetype of what YouTube's July 2025 "inauthentic content" policy demonetizes. Negative intel: this is the lane NOT to drift into; the operation's transformative layers (composites, measured edit grammar, original analysis, source credits) are the moat.

## What we apply
- **prehistoric_pov**: one-session TEST of Flow image mode + Nano Banana 2 as a supplemental $0 still source next to ChatGPT still-series (own Google account, no extension). Compare 10 stills for POV-style fidelity before Grok i2v.
- **rexcaped / bizdoc**: nothing changes. VO-first alignment already core; metadata/thumbnails already systematized deeper.
- **all**: do NOT install Zapi Flow; if bulk Flow gen ever matters, replicate with in-house browser automation.

## Verdict
Low-value for this operation. One tool datapoint worth a cheap test (Nano Banana 2 via Flow); everything else is either already done better in-pipeline or is the demonetization-risk archetype to avoid. The title's revenue claim is fiction.
